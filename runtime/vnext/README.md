# VNext Runtime

Runtime implementation space for OroTitan VNext.

Initial state: no production writer, no canonical publication authority, no production Registry mutation authority.

All runtime behavior must target the shadow environment until an explicit promotion gate changes this boundary.
