# VNext Tests

Test space for VNext state machines, contracts, schemas, deterministic gates, recovery behavior, golden fixtures, and shadow-run non-regression.

A failing deterministic assurance test blocks promotion.
